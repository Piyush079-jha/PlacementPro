```json
[
  {
    "id": "c1",
    "title": "Subarray Sum Equals K",
    "description": "Given an array of integers nums and an integer k, return the total number of subarrays whose elements sum up to k.

Input Format:
- The first line of input contains an integer n, denoting the size of nums array.
- The second line of input contains n space-separated integers representing the elements of the array.

Output Format:
- The first and only line of output should contain the total number of subarrays whose elements sum up to k.
k is also provided in the input. The value of k is given at the third line of the input.

Constraints:
- 1 <= k <= 100
- 1 <= nums.length <= 10000
- -100 <= nums[i] <= 100

Starter Code:
```javascript
// const readline = require('readline');

const rl = require('readline').createInterface({
  input: process.stdin,
  output: process.stdout
});

let input = [];
let currLine = 0;

rl.on('line', line => {
  input.push(line);
});

rl.on('close', () => {
  // write your code here
});

function readLine() {
  return input[currLine++];
}
```

Test Cases:
```json
[
  { "input": "3\n1 1 1\n2", "expectedOutput": "3", "hidden": false },
  { "input": "5\n2 2 1 1 2\n0", "expectedOutput": "3", "hidden": false },
  { "input": "10\n1 -2 2 1 -4 -5 1 2 2 1\n10", "expectedOutput": "16", "hidden": true }
]
```

  },
  {
    "id": "c2",
    "title": "Longest Consecutive Sequence",
    "description": "Given an unsorted array of integers, find the length of the longest consecutive elements sequence.

Input Format:
- The first line of input contains an integer n, denoting the size of nums array.
- The second line of input contains n space-separated integers representing the elements of the array.

Output Format:
- The first and only line of output should contain the length of the longest consecutive elements sequence.

Constraints:
- 1 <= nums.length <= 30000
- -109 <= nums[i] <= 109

Starter Code:
```javascript
// const readline = require('readline');

const rl = require('readline').createInterface({
  input: process.stdin,
  output: process.stdout
});

let input = [];
let currLine = 0;

rl.on('line', line => {
  input.push(line);
});

rl.on('close', () => {
  // write your code here
});

function readLine() {
  return input[currLine++];
}
```

Test Cases:
```json
[
  { "input": "10\n100 4 200 1 3 2", "expectedOutput": "4", "hidden": false },
  { "input": "5\n100 4 200 1 3 2 101 4 200", "expectedOutput": "3", "hidden": false },
  { "input": "10\n1 2 3 4 5 6 7 8 9 0", "expectedOutput": "9", "hidden": true }
]
```

]
```